# Tech Stack

- **Lang / Platform:** C# 14 / .NET 10
- **Results deps:** none — the package has no runtime dependencies beyond the BCL (`Microsoft.SourceLink.GitHub` is build-only)
- **Tests:** xUnit v3, coverlet, `TngTech.ArchUnitNET`
- **Package id:** `MSL.Results` (from `PackageId`; the assembly is `Results`)
